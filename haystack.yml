name: haystack
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - poppler
  - tesseract
  - pip
  - pip:
      - accelerate
      - bitsandbytes
      - chromadb
      - chroma-haystack
      - haystack-ai
#      - jq                          # json converter
      - libmagic
#      - markdown-it-py              # markdown converter
#      - mdit_plain                  # markdown converter
      - pillow
      - pymupdf4llm
      - pypdf2
#      - python-docx                 # docx converter
      - python-magic 
#      - python-pptx                 # pptx converter
      - sentence-transformers
      - streamlit
#     - tika                         # tika converter; also requires tika docker container
      - torch
      - torchvision
      - torchaudio
      - transformers
      - unstructured[all-docs]
      - unstructured-haystack
      - unstructured-fileconverter-haystack
      - vllm
      - vllm-haystack
